@{
    if (Context.Request.Path != "/" && Context.Request.Path.Value != "/Login/LoginB") // Ajusta la ruta según tu configuración
    {
        Layout = "_Layout"; // Desactiva el diseño para la vista de inicio de sesión
    }
}
@{
    if (Context.Request.Path != "/" 
    //&& Context.Request.Path != "/Login/CambioContrasenaEmail" 
    //&& Context.Request.Path != "/Login/CambioContrasenaPass"
    && !Context.Request.Path.ToString().Contains("Login")) // Ajusta la ruta según tu configuración
    {
        
        Layout = "_LayoutB"; // Desactiva el diseño para la vista de inicio de sesión
    }
}